export const stylesCreator = (theme) => ({
  Default: {
    root: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'left',
      margin: theme.spacing(2),
      width: 74,
    },
  },
});
