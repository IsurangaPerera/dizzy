version: '3.3'
services:
  proxy:
    container_name: proxy
    build:
      context: ./proxy
    links:
      - web
    ports:
      - '8080:80'
    restart: always
  mongo:
    container_name: 'mongo'
    image: mongo:5.0-focal
    volumes:
      - mongo-data:/data/db
      - mongo-config:/data/configdb
    restart: always
  api:
    container_name: api
    links:
      - mongo
    build:
      context: ./api
      dockerfile: Dockerfile.prod
    environment:
      NODE_ENV: 'production'
      HOST: '0.0.0.0'
      PORT: 5000
      RATE_LIMIT_WINDOW_MS: 60000
      RATE_LIMIT_MAX: 100
      MONGO_URI: 'mongodb://mongo:27017/dizzy'
      ES_URI: 'http://es.cibr.qcri.org'
      ES_PING_TIMEOUT_MS: 20000
      ES_CRAWLER_INDEX: 'darkweb-tor-index'
      ES_RECRAWLER_INDEX: 'darkweb-tor-recrawler-index'
      ES_HEALTHCHECK_INDEX_OLD: 'darkweb-healthcheck'
      ES_HEALTHCHECK_INDEX: 'darkweb-healthcheck-index'
      JWT_SECRET: 'UJl5Y8nK7b7TNjz8'
      JWT_AUTH_EXPIRE_DAYS: '30d'
      JWT_ACTIVATION_EXPIRE_DAYS: '3d'
      JWT_COOKIE_EXPIRE_MS: 259200000
      LOGOUT_COOKIE_EXPIRE_MS: 10000
      RESET_PASSWORD_EXPIRE_MS: 600000
      ACCOUNT_ACTIVIATION_URL: 'https://dizzy.cibr.qcri.org/activate'
      ACCOUNT_ACTIVIATION_EXPIRE_MS: 259200000
      SMTP_HOST: 'smtp.office365.com'
      SMTP_PORT: 587
      SMTP_EMAIL: 'dizzy.cibr@qcri.org'
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      FROM_EMAIL: 'dizzy.cibr@qcri.org'
      FROM_NAME: 'Dizzy'
    restart: always
  web:
    container_name: web
    depends_on:
      - api
    stdin_open: true
    build:
      context: ./web
      dockerfile: Dockerfile.prod
    environment:
      NODE_ENV: 'production'
    restart: always

volumes:
  mongo-data:
  mongo-config:
